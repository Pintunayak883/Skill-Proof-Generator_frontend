# Skill Proof Generator — Client

The frontend application for the Skill Proof Generator, a next-generation AI-powered skill assessment platform.

## 🚀 Tech Stack

-   **Framework:** Next.js 14 (App Router)
-   **Language:** TypeScript
-   **Styling:** Tailwind CSS + Headless UI
-   **State/Forms:** React Hook Form + Zod
-   **Code Editor:** `react-simple-code-editor` + `prismjs`
-   **Markdown:** `react-markdown` + `@tailwindcss/typography`

## ✨ Key Features

-   **HR Dashboard:** Create jobs, generate test links, view detailed candidate reports.
-   **Candidate Experience:**
    -   **Resume Parsing:** AI extracts skills to personalize the test.
    -   **3-Part Assessment:** Conceptual, Coding, and Scenario-based tasks generated by Gemini AI.
    -   **Pro Code Editor:** Dark mode editor with syntax highlighting.
-   **Integrity Monitoring:**
    -   **Live Camera:** Non-intrusive webcam monitoring.
    -   **Activity Tracking:** Detects tab switches, window blurs, and copy/paste.
    -   **Snapshots:** Captures evidence upon submission.

## 🛠️ Setup

1.  **Install Dependencies:**
    ```bash
    npm install
    ```

2.  **Configure Environment:**
    Create a `.env.local` file in the root:
    ```env
    NEXT_PUBLIC_API_URL=http://localhost:4000
    ```

3.  **Run Development Server:**
    ```bash
    npm run dev
    ```
    Open [http://localhost:3000](http://localhost:3000).

## 📂 Project Structure

-   `app/hr`: Authentication and HR dashboard routes.
-   `app/candidate`: Public candidate assessment flow.
-   `lib/antiCheat.ts`: Logic for integrity monitoring.
-   `lib/api.ts`: API client for backend communication.
